<div class="container">
  {{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
  {{/each}}
  {{#if noLayoutNeeded}}
  <Layout @profileName={{profile.profileName}} @action="invalidateSession">
    {{outlet}}
  </Layout>
  {{else}}
  {{outlet}}
  {{/if}}
</div>
