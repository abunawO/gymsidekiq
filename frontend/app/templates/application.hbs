<div class="container">
  {{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
  {{/each}}
  {{#if isNotIndex}}
  <Layout>
    {{outlet}}
  </Layout>
  {{else}}
  {{outlet}}
  {{/if}}
</div>