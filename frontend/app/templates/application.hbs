<div class="container">
  {{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
  {{/each}}
  {{#if noLayoutNeeded}}
  <Layout @profile={{profile}} @action="invalidateSession">
    {{outlet}}
  </Layout>
  {{else}}
  {{outlet}}
  {{/if}}
</div>
