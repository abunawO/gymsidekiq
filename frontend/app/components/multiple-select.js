//Takes a collection and displays a multi select element
import Ember from 'ember';
import Component from '@glimmer/component';

export default Ember.Component.extend({
  listOfCollection: [],

  onChange() {
    debugger
  }
});
